import { Metadata } from 'next'
import React from 'react'
export const metadata : Metadata ={
    title:"Projects || tamim portfolio",
    description:"All about tamim portfolio website"
}

const page = () => {
  return (
    <div>Projects</div>
  )
}

export default page
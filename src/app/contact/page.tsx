import { Metadata } from 'next'
import React from 'react'
export const metadata : Metadata ={
    title:"Contact || tamim portfolio",
    description:"All about tamim portfolio website"
}

const page = () => {
  return (
    <div>Contact</div>
  )
}

export default page